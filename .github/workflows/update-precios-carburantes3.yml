name: Actualizar Precios Carburantes

on:
  workflow_dispatch:           # Permite ejecución manual

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Descargar datos oficiales del Ministerio
        run: |
          mkdir -p docs
          curl -H "Accept: application/json" \
               -H "User-Agent: GitHub-Actions-PrecioCombustible" \
               -o docs/precios-carburantes.json \
               "https://sedeaplicaciones.minetur.gob.es/ServiciosRESTCarburantes/PreciosCarburantes/EstacionesTerrestres/"

      - name: Commit y Push si hay cambios
        uses: EndBug/add-and-commit@v9
        with:
          add: 'docs/precios-carburantes.json'
          message: 'Actualización automática de precios carburantes - ${{ format('{0,date,yyyy-MM-dd HH:mm}', github.event.repository.updated_at) }}'
          default_author: github_actions
          github_token: ${{ secrets.GITHUB_TOKEN }}   # ← Esto es importante
